{
	"extends": "../tsconfig.json",
	"compilerOptions": {
		// Language and Environment
		"target": "ESNext",
		
		// Modules
		"module": "ESNext",
		"moduleResolution": "Bundler"
	},
	"include": [
		"**/*.ts",
		"**/*.mts",
		"**/*.cts"
	],
	"exclude": [
	]
}
