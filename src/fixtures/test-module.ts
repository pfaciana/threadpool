/**
 * Simple test module with basic mathematical operations and a property.
 * Used for testing module imports and proxy functionality in workers.
 */
// Simple test module for testing purposes
export const add = (a: number, b: number): number => a + b
export const multiply = (a: number, b: number): number => a * b
export const someProperty: string = 'test value'
